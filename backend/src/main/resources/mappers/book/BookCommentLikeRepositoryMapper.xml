<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Recorders.ggogit.domain.book.repository.BookCommentLikeRepository">
    <insert id="save" parameterType="Recorders.ggogit.domain.book.entity.BookCommentLike">
        INSERT INTO BOOK_COMMENT_LIKE (MEMBER_ID, BOOK_COMMENT_ID, ACTIVATE)
        VALUES (#{memberId}, #{bookCommentId}, #{activate})
    </insert>

    <update id="udpate" parameterType="BookCommentLike">
        UPDATE BOOK_COMMENT_LIKE
        SET
            MEMBER_ID = #{memberId},
            BOOK_COMMENT_ID = #{bookCommentId},
            ACTIVATE = #{activate}
        WHERE ID = #{id}
    </update>
</mapper>