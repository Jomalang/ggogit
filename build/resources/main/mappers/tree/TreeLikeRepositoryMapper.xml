<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Recorders.ggogit.domain.tree.repository.TreeLikeRepository">
<!--트리 도서 row 생성-->
    <insert id="save" parameterType="TreeLike">
        INSERT INTO TREE_LIKE(MEMBER_ID, TREE_ID, ACTIVATE)
        VALUES(#{memberId},#{treeId},#{activate})
    </insert>



    <!--ID 별 트리 도서 정보 조회-->
    <select id="findById" resultType="TreeLike">
        SELECT *
        FROM TREE_LIKE
        WHERE
            MEMBER_ID=#{memberId} AND TREE_ID = #{treeId}
    </select>


    <!--ID 뱔 트리 도서 정보 수정-->
    <update id="update">
        UPDATE TREE_LIKE
        SET ACTIVATE=#{activate}
        WHERE
            MEMBER_ID=#{memberId} AND TREE_ID = #{treeId}
    </update>

    <delete id="delete" parameterType="TreeLike">
        DELETE FROM TREE_LIKE WHERE TREE_ID = #{treeId}
    </delete>
</mapper>