<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Recorders.ggogit.domain.leaf.entity.LeafCommentLike">

    <insert id="insertLeafCommentLike" parameterType="Recorders.ggogit.domain.leaf.entity.LeafCommentLike">
        INSERT INTO LEAF_COMMENT_LIKE (MEMBER_ID, LEAF_COMMENT_ID, ACTIVATE, UPDATE_TIME, CREATE_TIME)
        VALUES (#{memberId}, #{leafCommentId}, #{activate}, #{updateTime}, #{createTime})
    </insert>

    <update id="update" parameterType="LeafCommentLike">
        UPDATE LEAF_COMMENT_LIKE
        SET ACTIVATE = #{activate}
        WHERE MEMBER_ID = #{memberId}
            AND LEAF_COMMENT_ID = #{leafCommentId}
    </update>

</mapper>