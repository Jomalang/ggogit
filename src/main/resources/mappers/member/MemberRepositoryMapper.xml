<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Recorders.ggogit.domain.member.repository.MemberRepository">

    <insert id="save" parameterType="Member">
        INSERT INTO MEMBER(EMAIL, PASSWORD, NICKNAME, INTRODUCTION)
        VALUES(#{email}, #{password}, #{nickname}, #{introduction})
    </insert>

    <update id="update" parameterType="Member">
        UPDATE MEMBER
        SET EMAIL = #{email}, PASSWORD = #{password}, NICKNAME = #{nickname}, INTRODUCTION = #{introduction}
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="Member">
        DELETE MEMBER
        WHERE ID = #{id}
    </delete>

    <select id="findAll" resultType="Member">
        SELECT * FROM MEMBER
    </select>

    <select id="findById" resultType="Member">
        SELECT * FROM MEMBER
        WHERE ID = #{id}
    </select>

    <select id="findByEmail" resultType="Member">
        SELECT * FROM MEMBER
        WHERE EMAIL = #{email}
    </select>

    <select id="findByName" resultType="Member">
        SELECT * FROM MEMBER
        WHERE NAME LIKE '%${name}%'
    </select>

</mapper>